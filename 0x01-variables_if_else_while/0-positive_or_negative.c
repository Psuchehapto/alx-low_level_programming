#include <stdio.h>
#include <stdlib.h>
#include <time.h>
/**
 * main - Entry point
 *
 * Return: Always 0
 */
int main(void)
{
	int n,

	    srand(time(0));
	n = rand() - RAND_MAX / 2,
	  (n > 0)
		  intf("%d is positive \n", n);
	se if (n < 0)
		intf("%d is negative\n", n);
	se
		intf("%d is nzreo\n", n);
	turn (0);
}
